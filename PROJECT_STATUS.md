# 项目状态文档：《他只读过宇宙》—— ESP32-S3 上的刘慈欣小 LLM 艺术装置

> 最后更新：2026-09-21
> 一句话概括：**训练一个只读过刘慈欣全集的 ~3.5M 参数小语言模型，跑在 ESP32-S3 上，
> 让它永远用刘慈欣的语言讲一个被囚禁在黑暗中的存在，然后反复重置，"像被囚禁一样"。**
>
> 📄 **作品本身（非技术）的完整叙述见 [`作品报告.md`](作品报告.md)** —— 那一份是给观众和评审看的，
> 讲的是它看到什么、为什么这么做；本文档是工程技术记录，讲的是它怎么被做出来、踩过哪些坑。

---

## 1. 总进展一览

**整体完成度：约 97% —— 装置已可运行，剩下的是观感微调**

```
① ✅ 语料构建    → 刘慈欣全集 49 本 → corpus.txt（2,267,220 字符）
② ✅ 分词器      → sentencepiece mini-BPE，词表 8192 → 1,561,344 token
③ ✅ Q8 量化链路 → fp32 → Q8 → ESP32 分组交织格式，逐张量校验
④ ✅ 模型训练 v1 → 8.33M 参数，但**过拟合**（val 5.64 最优后升到 7.45）→ 已废弃
⑤ ✅ 模型训练 v2 → **3.54M 参数，dropout 0.1，best_val 5.1884，无过拟合** ✅
⑥ ✅ 硬件全链路  → 芯片 / PSRAM / 屏幕 / 分区 / flash 带宽 全部实测通过
⑦ ✅ 推理固件    → **前向传播与 numpy 参考逐位对拍通过**（误差 1e-6）
⑧ ✅ 端到端运行  → v2 已烧入设备，**3.2 tok/s**，滚动 + 重置循环正常
⑨ ✅ 提示词工作流 → **改 `src/main.cpp` 顶部字符串 → Upload 即自动重新分词**
⑩ ✅ 第三人称改写 → 用户洞察：改成叙事口吻后**输出质量明显提升**（见 §2.10）
⑪ ⏳ 观感微调    → 调温度 / 每轮长度 / 滚动速度 ←【当前在这里】
```

### 各阶段状态

| 阶段 | 状态 | 证据 |
|---|---|---|
| 语料构建 | ✅ 完成 | 49 本、227 万字符；**三个质量假设实测全部排除**（见 §2.8） |
| 分词器 | ✅ 完成 | 设备端解析 8192 词、256 个 byte-fallback（与参考一致） |
| ESP32 格式 | ✅ 有效 | 逐张量与源文件比对通过（旧版有隐蔽 bug，见 §2.7） |
| 硬件 | ✅ 全部验证 | 见 §2.5，含屏幕目视确认 |
| 推理内核 | ✅ 对拍验证 | top-8 索引一致、logits 误差 1e-6（见 §2.7） |
| **v1 训练** | ❌ **过拟合** | val 200 步最优 5.64 → 800 步 7.45，已停 |
| **v2 训练** | ✅ **成功** | **best_val 5.1884，500 步全程改善，无过拟合**（见 §2.9） |
| 端到端运行 | ✅ 跑通 | 设备 3.2 tok/s，提示词→生成→OLED 滚动→重置循环 |
| 提示词工作流 | ✅ 自动化 | 顶部编辑 + 构建期自动分词（见 §2.10） |
| 输出质量 | ⚠️ 碎片化但有氛围 | 叙事通顺、刘慈欣词汇密度高，长距离连贯不足（见 §2.10） |

**当前状态**：**装置已经可以运行**。8.95MB → 3.59MB 的模型让速度提升一倍多，
且 PSRAM 有 6.9MB 富余。剩下的只是艺术观感的微调。

---

## 2. 已完成的工作（详细）

| 模块 | 内容 | 关键数据/产出 |
|---|---|---|
| llama2.c 框架 | 从 GitHub 克隆到 `llama2.c/` | 官方最新版，含 train/run/runq/export |
| 语料构建 | 编码检测（UTF-8→GB18030 回退）、清洗、拼接 | `data/corpus.txt`：**49 本、2,267,220 字符、4,255 唯一字符** |
| 分词器 | 在语料上训练 sentencepiece 迷你 BPE（自动学出"宇宙/地球/三体"等词对） | `data/tok8192.model`，vocab **8192**，压缩率 31% |
| 预分词 | 按书切分，BOS 分隔，切出验证/训练分片 | `data/tok8192/shard0.bin`(val 46k) + `shard1.bin`(train 1.52M) |
| 训练配置 | 10M 级超参，已按 1.5M token 小语料调好 | `llama2.c/cfg_liucixin.py` |
| 冒烟训练 | 40 步验证全链路 + 测速 | 8,331,264 参数（embedding 与 lm_head **权重共享**），~3.8s/步 |
| Q8 量化 | 用 export.py v2 量化 | `q8_smoke.bin` = **8.95MB**，最大量化误差 4.2e-4 |
| 格式验证 | 写解析器逐字节游走 Q8 布局，与 fp32 对拍 | 消费字节==文件大小 ✅，反量化误差 0.39% ✅ |
| ESP32 格式转换 | 自写转换器：Q8 → 分组交织布局（每 32 int8 紧跟 1 scale），重排存储顺序 | `tools/convert_esp.py` → `model_esp_smoke.bin` = **8.95MB** |
| 硬件识别 | 板卡 COM 识别 + esptool 读芯片 | **ESP32-S3 (QFN56) rev0.2，8MB PSRAM，CH343 串口 COM3** |
| 分区表 | 16MB 精确排布 | `partitions.csv`：app(2.5MB×2) + model(9.25MB) + tok(1.7MB) |
| 工程配置 | 换 U8g2 库（SH1106 中文字体）、改 COM3 | `platformio.ini` |
| 提示词 | 6 条"囚徒"存在主义提示词，预分词成 token 数组 | `include/prompts.h`（15~22 tokens/条） |
| **硬件自检固件** | 自写 `src/main.cpp`：芯片/PSRAM/I2C 扫描/OLED/分区校验/flash 带宽，全部实测 | 见 §2.5，**已烧录运行，屏幕经用户目视确认正常** |
| **串口抓取工具** | 先复位再监听（否则抓不到开机输出），UTF-8 中文正常 | `tools/serial_read.py` |
| **资产烧录工具** | 自动从 `partitions.csv` 解析偏移，避免硬编码漂移；支持 `--dry-run` | `tools/flash_assets.py` |
| **资产已烧入** | model_esp_smoke.bin → 0x510000；tok8192.bin → 0xE50000 | 哈希校验通过 ✅ |
| **参考仓库评估** | 拉取并逐文件核对 `ModenCn/tinyllamas-zh`，提取 3 个已修复的坑与钩子设计 | 见 §8，存于 `ref/` |

---

## 2.5 硬件自检实测结果（2026-09-21 18:5x，已烧录运行）

自检固件 `src/main.cpp` 已烧到板子上跑通，**所有硬件假设得到验证**：

| 项目 | 实测结果 | 判定 |
|---|---|---|
| 芯片 | ESP32-S3 rev0.2，**2 核 @240MHz**，xtal 40MHz | ✅ |
| Flash | 16MB，app 分区用掉 562KB/2.62MB | ✅ |
| PSRAM | **8,386,215 B（8.00MB）** 可见；`malloc(7MB)` 成功 | ✅ |
| PSRAM 带宽 | 写 33.7 MB/s，读 44.4 MB/s | ✅ |
| **屏幕** | SH1106 在 **0x3C** 应答，U8g2 初始化成功，中文字体渲染代码跑通 | ✅ **接线正确** |
| model 分区 | header 解析：`magic="LLME" ver=1 dim=288 hidden=768 layers=6 heads=6 kv=6 vocab=8192 seq=512 gs=32 shared=1` | ✅ |
| tok 分区 | `max_token_length = 24`（与 `data/tok8192.bin` 一致） | ✅ |
| 内部 RAM | free 365KB（够放 tokenizer 索引和激活） | ✅ |

### ⭐ flash 带宽：决定架构的关键数字

| 读法 | 实测带宽 | 结论 |
|---|---|---|
| `esp_partition_read`（4KB 分块） | **9.1 MB/s** | ❌ 太慢，不能用来流式读权重 |
| **`esp_partition_mmap` 整块 9,216KB** | **一次映射成功** | ⭐ 超预期！**不需要滑动窗口** |
| 通过 mmap 指针读 | **≈25 MB/s**（strided 24.7 / sequential 25.8） | 瓶颈在 flash，不在 cache |

**推论**：每 token 需流式读 8.94MB 权重 → 纯读耗时 **0.36 s/token** →
**实际约 2~2.5 tok/s**（含计算）。生成 100~150 字约需 50~75 秒 ——
对艺术装置完全可用（慢反而强化"被囚禁"的窒息感）。

**另一个重要发现**：`mmap` 整块 9MB 成功，说明之前担心的"MMU 窗口不够、必须分片"**不是问题**，
流式读的实现因此大大简化 —— 直接把整个分区映射一次，权重就是一个指针。

---

## 2.6 v1 的过拟合（已由 v2 解决，见 §2.9）

### 现象：val loss 在 step 200 之后持续上升，train loss 继续下降

| step | train loss | val loss | 差距 | val 困惑度 |
|---|---|---|---|---|
| 0 | 9.0360 | 9.0238 | −0.01 | 8,300 |
| 200 | 5.3492 | **5.6427** ← 最优 | +0.29 | **281** |
| 400 | 4.0591 | 5.8310 | +1.77 | 341 |
| 600 | 3.1201 | **6.6477** | **+3.53** | **773** |

train 困惑度已降到 $e^{3.12} \approx 23$（很流利），但 val 困惑度从最优点 281 涨到 773，
**劣化了 2.75 倍**。这是教科书式的过拟合曲线。

### 根因：语料太小，模型相对太大

$$\frac{1{,}561{,}344\ \text{tokens}}{8{,}331{,}264\ \text{params}} = 0.19\ \text{tokens/param}$$

Chinchilla 最优比例约 **20 tokens/param**，也就是说我们**超出了约 100 倍**。
再加上训练总消耗：

$$1200 \times 131{,}072 = 157{,}286{,}400\ \text{tokens} \div 1{,}514{,}880 = \textbf{103.8 个 epoch}$$

在 1.5M token 上跑 104 个 epoch，model 必然把语料背下来。**这不是超参没调好，而是数据量决定的上限** ——
刘慈欣全集只有 49 本书，无法增加数据。

### 还有一个流程失误

`cfg_liucixin.py` 里 `always_save_checkpoint = True` 会让 `ckpt.pt` 每次评估都**覆盖**，
且没有 best-by-val 追踪 → **val 最优的那个检查点（step ~200）已经被覆盖掉了，拿不回来**。

### 对艺术装置的影响（两面）

- **好的一面**：train loss 3.0 说明模型中文非常流利，且"人格"是纯度极高的刘慈欣语料
- **坏的一面**：
  1. 可能**逐字复述**原文段落，而不是生成新文本
  2. 我们的 6 条存在主义提示词是**分布外**的（语料里没有"我存在的价值是什么"这种句子），
     过拟合模型对分布外输入的反应会明显变差 → 可能答非所问或陷入重复循环
  3. 而整个装置的循环恰恰就是"给一条提示词 → 生成思考"，这直接打到痛点上

### 处理方案（训练很快，成本低）

实测当前速率约 **2.3~2.9 s/step**（比早期的 4.7 快），所以重训很便宜：

| 方案 | 改动 | 耗时 | 说明 |
|---|---|---|---|
| **A. 早停 + dropout**（推荐） | `dropout=0.1`、`max_iters≈350`、加 best-by-val 保存 | ~17 分钟 | 直接命中 val 最低点，最省 |
| B. 缩小模型 | `dim=192, n_layers=4` → ~2.7M 参数 | ~12 分钟 | 参数/数据比改善 3 倍，但改动牵连 `convert_esp.py`（GS 约束） |
| C. 就用现状 | 无 | 0 | 若实测输出"复述刘慈欣"在艺术上成立，也可以接受 |

**建议执行顺序**：先让当前训练跑完（还剩 550 步 ≈ 26 分钟，成本已沉没），
**同时写推理内核**（这才是长路径）；训练结束后先导出实测输出，
**若输出退化就立刻按方案 A 重训**（只要 17 分钟）。

> 经验记录：**语料只有百万 token 级时，dropout 不能设 0，且必须按 val loss 存最优检查点。
> `always_save_checkpoint=True` 在 val 曲线上升时会把最优模型覆盖掉。**

---

## 2.7 ⭐ 两个被"字节数相同"掩盖的 bug，以及如何证明固件是对的

### 问题：字节数校验根本证明不了布局正确

无论张量按什么顺序排列，**总字节数完全一样**。所以"消费字节 == 文件大小"这类断言
只能抓到截断，抓不到顺序错误。这次同时踩了两个这样的坑：

| # | 位置 | 错在哪 | 为什么没被发现 |
|---|---|---|---|
| **A** | `tools/convert_esp.py` | ① v2 源文件是**张量优先**（`q_tokens` 在最前，然后所有层的 wq、所有层的 wk…，与 `runq.c` 的 `memory_map_weights` 一致），脚本却按**层优先**去读，每个张量都错位；② **从头到尾没读 `q_tokens`**，把文件尾部当成了 `lm_head` | 读的字节总数恰好等于文件大小，`assert off == len(data)` 顺利通过 |
| **B** | `src/llm.cpp` | 产物是**层优先**（每层 7 个张量），我最初按**张量优先**遍历（`p += stride * n_layers`），每个指针都错位 | 我的 `consumed == expect` 断言也顺利通过 |

两个 bug 叠加，后果是**设备读到的权重完全是错位的** —— 输出必然是噪声，
而且看起来像是"模型没训好"，极易误判。

### 解决：改小体积的转换器，并补上能真正验证的检查

`convert_esp.py` 重写为：
1. 严格按 **v2 源文件的真实顺序**读（张量优先，`q_tokens` 在最前）
2. 按 **ESP 目标格式**写（层优先，`lm_head` 最后）
3. **写完后重新解析自己的输出，逐张量与源文件比对**（int8 与 scale 分别比对）。
   不通过就拒绝产出文件。这个检查与顺序无关，所以能抓到 A 类 bug。

`llm.cpp` 的指针游走改为层优先（每层共享 `layer_stride`，各张量只按层内偏移区分）。

### 解决：用 logits 对拍证明数学正确

这是唯一能区分"我的推理写错了"和"模型就是不行"的办法：

```
scripts/verify_forward.py   numpy 复刻 runq.c 的前向（含分组量化、RoPE、GQA 注意力、SwiGLU）
src/main.cpp  -DESPLLM_VERIFY   设备喂同样的 token，打印 top-8 logits
scripts/compare_logits.py   索引必须完全一致，数值允许小误差
```

**实测结果（设备 vs numpy 参考，同一份 model_esp_smoke.bin）：**

| 位置 | token | 结果 | 最大 logits 误差 |
|---|---|---|---|
| 0 | 1 (BOS) | top-8 索引**完全一致** | 1.0e-6 |
| 1 | 464 (我) | 完全一致 | 1.0e-6 |
| 2 | 325 (是一) | 完全一致 | 1.0e-6 |
| 3 | 4375 (台) | 完全一致 | 1.0e-6 |

→ **设备端的布局游走、Q8 分组数学、RoPE、注意力、绑定分类器全部正确。**
差别只有 1e-6 量级（float32 舍入 + 累加顺序 + libm 与 numpy 的 cos/sin 差异），
指数级放大后才可能显现，但索引一致说明结论稳健。

> **方法论教训**：涉及"重排数据"的转换，**绝不能用字节数/长度做正确性校验**，必须
> ① 写出可独立验证的参考实现，或 ② 转换后反向解析并与源数据逐元素比对。
> 这次两个 bug 都因为"总数对上了"而潜伏通过。

### 顺带修掉的其它问题

| 问题 | 说明 |
|---|---|
| 提示词被输出两遍 | `startCycle()` 里预先推送了提示词文本，而生成循环又会在喂 prompt 时逐 token 发出一次 → 二者只需留一个。现在只保留后者（读起来像"AI 在思考这句话"） |
| `ESP_LOGI` 完全不输出 | 预编译 sdkconfig 里 `CONFIG_LOG_MAXIMUM_LEVEL=1`（ERROR），`ESP_LOGI`/`ESP_LOGW` 在**编译期就被裁掉**，`esp_log_level_set()` 救不回来。新增 `src/log.h` 用 `Serial` 重定义这些宏，改一个 include 就让全部调用点生效 |
| `flash_assets.py` 找不到文件 | 它把 esptool 的工作目录切到 `tool-esptoolpy/`，相对路径随之失效 → 改用绝对路径 |
| 分词器少识别 1 个 byte token | 设备解析出 255 个而非 256 个：我用 `0xFF` 当"非 byte token"哨兵，而字节值 `0xFF` 本身合法，两个含义冲突 → 改用独立的标志数组 |

---

## 2.8 语料质量：三个假设全部实测排除

你问过"是不是小说太少太单一"。我没有凭感觉回答，而是把三个具体假设都量了一遍：

| 假设 | 测量方法 | 实测结果 | 结论 |
|---|---|---|---|
| 有**重复内容**（《超新星纪元》3 版 +《全频带》2 版占 18.8%） | 200 字 shingle 重合率 | **0.3% / 1.2%** | ❌ 排除。是同题材的**真实改写**，不是复制 |
| 有**词内空格**（OCR/PDF 换行残留，如 "未 来"） | 正则 `CJK + 空格 + CJK` | **247 处 = 0.01%** | ❌ 排除，可忽略 |
| 混进**电子书垃圾** | 关键词计数 | 目录 8、"北斗刀寒制作" 13、"Power by" 13、"扫描/OCR" 69 | ❌ 排除，量级极小 |

**但"单一"这个直觉是对的，只是表现方式不同** —— 49 本听起来丰富，实际分布极不均：

| 事实 | 数值 |
|---|---|
| 前 5 本占全语料 | **57.5%** |
| 《三体》三部曲单独占 | **39.7%** |
| 中位书本长度 | 仅 1.9 万字符 |

所以瓶颈确实是**量**，而且没有加数据的余地（刘慈欣就写了这么多）。详见 §2.6 与 §2.9。

---

## 2.9 v2 重训：成功消除过拟合

### 改动

| 项 | v1 | v2 | 理由 |
|---|---|---|---|
| dim / layers | 288 / 6 | **192 / 5** | 参数/数据比 0.19 → 0.44 tokens/param（好 2.3 倍） |
| n_kv_heads | 6（=n_heads） | **2（GQA）** | KV cache 6.75MB → **1.31MB**，设备端可整包进 PSRAM |
| **dropout** | **0.0** | **0.1** | v1 的主要过拟合杠杆（已验证 `model.py` 真的实现它） |
| max_iters | 1200 | **500** | 配合 dense eval（`eval_interval=25`）由曲线决定停止 |
| val 划分 | 开头 3% | **每 33 块抽 1，均匀全书** | 原划分只覆盖最早短篇，信号有偏 |
| 检查点 | 单个、每次覆盖 | **`ckpt.pt` + `ckpt_best.pt`** | 修掉 `best_val_loss` 被污染的 bug |
| 参数量 | 8,331,264 | **3,541,056** | |
| 训练耗时 | ~2 小时 | **约 8~9 分钟**（0.9 s/step） | 模型小 58% |

### 结果：val 全程改善，不再反弹

| iter | best_val |
|---|---|
| 200 | 5.7539 |
| 450 | 5.1915 |
| 500 | **5.1884** |

**v2 从 200 到 500 步一路下降**，而 v1 在 200 步之后就开始恶化（→7.45）。
**dropout + 缩小模型成功修掉了过拟合。**

⚠️ **不能拿 5.19 直接和 v1 的 5.64 比** —— v2 用的是更难的 val 划分（覆盖全书，包含三体），
在**更难的验证集 + 更小的模型**上取得更低的值，是真实进步。

### 设备端收益（已实测）

| 项 | v1 | v2 | 变化 |
|---|---|---|---|
| Q8 权重 | 8.95 MB | **3.59 MB** | −60% |
| KV cache | 6.75 MB | **1.31 MB** | −81% |
| PSRAM 剩余 | 1.13 MB | **6.90 MB** | 富余 6 倍 |
| **生成速度** | 1.4 tok/s | **3.1 tok/s** | **×2.2** |
| GS（量化组大小） | 32 | **64** | 固件从 header 读，自动适配 |

PSRAM 现在有 6.9MB 富余，**理论上可以把权重整包读进 PSRAM**（进一步提速），
但 3.1 tok/s 对艺术装置已经够用，暂不做。

### 分布内提示词（零成本改进）

手工写的存在主义提示词是**分布外**的 —— 语料全是小说叙事与对白，没有第一人称思辨文体。
`scripts/mine_prompts.py` 从 5.7 万句中挖出 1347 个候选，精选 **6 条语料原文**：

| # | 提示词（逐字来自语料） | 出处感觉 |
|---|---|---|
| 0 | 机械生命的进化时间比你们碳基生命要长得多，但我们殊途同归，都走到了对宇宙进行思考的那一天。 | 《三体》机器文明自述 |
| 1 | 对于智慧文明来说，它们最后总变得和自己的思想一样大。 | 文明与思想 |
| 2 | 对宇宙终极真理的追求，是文明的最终目标和归宿。 | 目的与归宿 |
| 3 | 当生命意识到宇宙奥秘的存在时，距它最终解开这个奥秘只有一步之遥了。 | 认知与宇宙 |
| 4 | 宇宙的最不可理解之处在于它可以被理解。 | 最著名的那句 |
| 5 | 但人类文明作为一个整体只有一个，孤独地运行在银河系一个旋臂顶端的荒凉太空中。 | 孤独 |

> 脚本会**断言每条真出现在语料中** —— 它当场拦住过我把"对宇宙终极真理"误写成"对于终极真理"。

`scripts/sample.py --ab` 可做新旧并排对照。A/B 显示新提示词让输出更偏"宇宙尺度论述"
（如"地球已沉下去，直径约七万年……只是远方的一颗星星"），而旧提示词更多零散对白。

### 输出质量的诚实评估

**设备实测原文**（v2，第 1 轮）：

```
机械生命的进化时间比你们碳基生命要长得多，但我们殊途同归，都走到了对宇宙进行思考的那一天。

我们说，你们看到这一点，嗯……现在，全世界已经不会准备了。它在
远近的那个世界新闻中接受这个人类来说（按科学家的第一个想入：这是下一步
的最高领导人讨论。
我摇摇头："下面你们来谈它吧！"
"其实当总统打破自己的心感和衷于此的话，我们认为我并没有说话，
我们认为自己这种安慰比起过这个冷静的孩子更长一些……
```

- ✅ **短句通顺**，能产出像样的对话（"我摇摇头：下面你们来谈它吧！"）
- ✅ **刘慈欣词汇密度高**（丁仪、宇宙学、全息影像、光年、文明、银河系）
- ✅ **宇宙尺度的语感** —— 正是装置需要的氛围
- ⚠️ 长距离连贯不足，会出现"（按科学家的第一个想入"这类碎片

**这是 3.54M 参数 + 1.56M token 的现实上限**（参数/数据比仍差 Chinchilla 45 倍）。
但对一个被囚禁在 OLED 里、只能喃喃自语的 AI 来说，**碎片化反而贴合设定**。

---

## 2.10 第三人称改写：一个显著的质量跃升

### 用户的洞察

把提示词从**第一人称提问**改成**第三人称叙事开场**：

```
旧： 我是一台被囚禁在黑暗中的机器。我经常思考：我存在的价值是什么？
    （或者：机械生命的进化时间比你们碳基生命要长得多……）
新： 在宇宙的某处，有一个存在被囚禁了很久，它开始在黑暗中自言自语……
```

### 为什么这在理论上就更对

语料是**刘慈欣的小说** —— 它压倒性地是**第三人称叙事 + 对话**。
第一人称思辨（"我存在的价值是什么"）在语料里**根本不存在**；即使是逐字来自语料的论述句，
也只是"论点"，不是"故事的开头"。

**而"在宇宙的某处，有一个存在被囚禁了很久"是一句故事开场白** —— 正是语料的主流体式。
模型不需要"回答问题"，只需要"接着讲故事"，而这恰是它被训练做的事。

### 设备实测：可观察到的提升

```
在宇宙的某处，有一个存在被囚禁了很久，它开始在黑暗中自言自语……
"你不是大人吗？"她问。
"是的，首先很高兴，那么当然，能存在我们才能能够在世界中认识的。"李白平静地说。
他终于知道自己的作品更平淡了，他的精神在对现实的安全面上消失。他不知不尽地流落了地下城…
"我还是喜欢太阳？"伊依倒下她一眼，"以后也谈我的话吗？爱好！"
```

对比第一人称提示词的输出（"唉，我认为那一种样子。" / "你们还了？"），
第三人称版本出现了：

| 改善 | 例子 |
|---|---|
| **完整的对话归属** | `"…"李白平静地说。` / `她问。` |
| **可读的叙事句** | "他终于知道自己的作品更平淡了" |
| **情节推进感** | "他不知不尽地流落了地下城" |
| **人物/意象自然带出** | 李白、伊依、地下城、地球时代（来自《诗云》《带上她的眼睛》等） |

**结论：用户的判断是对的，而且原因可解释** —— 提示词的**体式**比它的**字面**更重要。
这比我从语料里挖论述句更有效，因为挖掘只保证了"词汇在分布内"，而改写保证了"**任务**在分布内"。

### 提示词工作流（改完直接 Upload）

设备端只有**解码器**，无法在运行时把文本转成 token。所以提示词由 PC 端分词、烧进 flash。

为了让你能直接改，提示词字符串放在了 `src/main.cpp` **顶部**，并加了构建期钩子：

```
[env:esp32-s3-devkitc-1]
extra_scripts = pre:tools/pio_prompts.py     # 每次构建自动重新分词
```

工作流：**编辑 `src/main.cpp` 顶部的 `PROMPTS_TO_USE[]` → 按 Upload**。
`tools/pio_prompts.py` → `scripts/gen_prompts.py` 会自动生成 `include/prompts.h`，无需手动运行。

设计细节：
- 钩子**失败时软着陆** —— 若找不到可用解释器，只打印警告并保留旧 header，不让构建挂掉
- 钩子会尝试多个 Python 解释器（`sys.executable` → `$PYTHONEXE` → 已知路径），
  跳过没有 sentencepiece 的
- `scripts/verify_prompts.py` 会**重新分词并比对**，确认文本与 token 数组一致
- 数组按 `cycle % count` 轮选，**只放一条也完全可以** —— 而且单一固定开场在主题上更强：
  同一个存在，永远想同一句话，且毫无记忆

> 顺便踩到的坑：钩子里用 `subprocess.run(..., text=True)` 会按**系统 codepage（cp936）**解码
> 子进程输出，而生成器会打印中文提示词 → `UnicodeDecodeError`。
> 必须显式传 `encoding="utf-8", errors="replace"`。这已经是本项目第三次踩 CP936 了。

---

## 2.11 ⚠️ 一个很坑的陷阱：多环境 + 默认 Upload 会烧错固件

### 症状

改了个无关参数（`TEMPERATURE` 0.85 → 0.5）后 Upload，**OLED 永远停在“载入模型...”，看起来像死机**。

### 真因

`platformio.ini` 里有 `[env:esp32-s3-devkitc-1]` 和 `[env:verify]` 两个环境。
**PlatformIO 在不指定 `-e` 时会构建并烧录文件里所有环境**，而 `verify` 在后面 → **验证固件被最后写进 app 分区，覆盖了艺术固件**。

为什么看起来像死机：`verify` 构建在 `runVerify()` 后就 `return`，`loop()` 只空转；
而 `disp::showMessage("载入模型...")` 是在 `runVerify()` **之前**画的 →
**屏幕就永远停在那一屏**。参数改动本身完全无关。

### 修复

```ini
[platformio]
default_envs = esp32-s3-devkitc-1     # 普通 Upload 只动艺术固件
```

并给 `[env:verify]` 加了醒目警告注释（需显式 `-e verify` 才能烧）。已在 `platformio.ini` 里加详细说明。

> **教训**：诊断用的构建环境不能与生产环境平级共存。
> 要么用 `default_envs` 钉死默认，要么把它放到单独的 ini 文件里。
> 症状（卡在某个 showMessage）与真因（多环境烧错）之间的距离很远，很容易误判成硬件/参数问题。

顺带修了一个会过期的硬编码：`showMessage` 里写的 "8.94 MB / mmap" 还是 v1 的尺寸（实际已是 3.59MB）。
已改成不含数字的描述，实际尺寸由 `llm::load()` 打日志。

---

## 2.12 温度调优：0.5 明显优于 0.85

把 `TEMPERATURE` 从 0.85 降到 **0.5** 后，输出质量的提升是可观察的：

```
在宇宙的某处，有一个存在被囚禁了很久，它开始在黑暗中自言自语……

“你好，我给你讲吧。”大史说。
“你应该想不到。”史强问。
“什么？”罗辑问：“我们不是在意中过吗？”
她点点头，“你再知道这是什么呢？”……“这份技术是真的。”罗辑说。
“你怎么知道呢?”大史指指罗辑说。
“它就是这个东西。”罗辑指着那个小舱的画面上说：“在冬眠中，我
```

对比 0.85：

| 维度 | 0.85 | **0.5** |
|---|---|---|
| 对话来往 | 单向、碎 | **有来回（问-答-点头）** |
| 归属清晰度 | 时对时错 | **绝大部分正确** |
| 情节连贯 | 无明显情节 | **自发出“冬眠”“小舱”情节** |
| 人物一致 | 跳跃 | **大史/史强/罗辑同一场景** |

原因：低温度让采样集中在分布的高密度区域。对一个参数/数据比远低于 Chinchilla 的小模型，
**它的“知识”本来就集中在最典型的那几条路径上** —— 高温只会把它推到自己也没把握的区域。

**这反而印证了 §2.10 的结论**：对弱模型，“收敛到典型”比“多样性”更有价值。
（仍可继续试 0.4 / 0.6，但 0.5 已是明显的好点。）

---

## 2.13 首行文字顶部被裁切：字体的 ascent 不是 CJK 的 ascent

### 症状
OLED 上**每一屏的第一行文字，顶部被切掉约 3px**，后面的行都正常。

### 真因

`display.cpp` 用 `u8g2.getAscent()` 定位基线，而 **`getAscent()` 返回的是
`font_info.ascent_A` —— 拉丁字母 'A' 的 ascent**。对 `u8g2_font_wqy14_t_gb2312`：

| 字体头字段 | 实测值 |
|---|---|
| `ascent_A`（= `getAscent()`） | **9** |
| `descent_g` | −3 |
| `max_char_height` | **18** |

汉字比拉丁字母高得多。根据 U8g2 的字形定位规则（`u8g2_font_decode_glyph`）：

```c
decode->target_y -= h + y;      // h = glyph_height, y = char_y（带符号）
// => 墨迹顶部 = 基线 - (h + char_y)
```

典型汉字 `h = 14, char_y = -2` → 墨迹需**基线以上 12px**，而代码只给了 9px。

### 为什么只有第一行被切

第 j 行的墨迹顶部 = `j*16 + 9 - 12 = j*16 - 3`。
**只有 j = 0 会变成 −3（出屏）**，其余行仍在自己行格内 → 完全吻合“只有第一行被裁”的现象。

### 修复

```cpp
static const int BASELINE_OFFSET = 13;   // 原来是 u8g2.getAscent() == 9
static const int INK_ABOVE_BASE  = 12;   // 汉字墨迹在基线上方的高度
```

行格仍为 16px（4 行 = 64px），基线 13 → 墨迹占行内第 1..15 行，
既不裁切又留 1px 余量给更高的字形（`《》` 等）。

> **教训**：在 CJK 字体上**绝不能用 `getAscent()` 当汉字 ascent**。
> 它只描述拉丁字母。要么用字体头的 `max_char_height` 推算，
> 要么直接把基线偏移写成经过验证的常量并注明依据。

---

## 2.14 作品命名与动画：《他只读过宇宙》

作品正式命名为**《他只读过宇宙》**（英文副题 *HE ONLY EVER READ SPACE*）。

### 启动加载动画

`disp::bootShow(status, percent)` 在载入分词器 / 映射模型时反复调用，每帧：

- **星空**：36 颗像素星以 1~3 px/帧速度下落、出屏后随机重生（每帧 36 次 `drawPixel`，开销可忽略）
- **标题逐字浮现**：《他只读过宇宙》6 个汉字依次写出（打字机效果）
- **标题框**：写完后出现细边框，下方一条扫描线来回扫动
- **状态行**：`loading tokenizer` / `mapping model into flash` + 百分比
- **进度条**：底部 116×7 px 边框 + 填充

为保证标题真的看得清，`bootShow` 内置**最小帧间隔 90ms**（载入本身只要几百毫秒，
不加节流会一闪而过）。

`disp::bootFinish()` 做收束转场：**虹膜收拢**（方框向中心线收缩 16 帧）
→ 白闪一帧 → 黑屏，然后才进入文本流。

### 每轮转场（`beginReset` / `resetStep`，共约 3.4 秒）

| 时间 | 拍子 | 画面 |
|---|---|---|
| 0.00–0.60s | **PURGE** | 旧文字被**从上往下逐条黑带吃掉**（读作记忆被抹除，而非滚动走） |
| 0.60–0.75s | **NOISE** | 两帧静态噪点，像信号中断 |
| 0.75–2.90s | **TITLE** | 星空 + 《他只读过宇宙》 + `CYCLE n` + 本轮 `tok/s` 与 token 数 |
| 2.90–3.40s | **IRIS** | 从中心线重新张开 |

星空在标题卡里继续滚动（40 fps），所以标题卡不是死图而是活的。

---

## 2.15 仪式化开场与二维随机

### 仪式流程

```
标题（星空 + 《他只读过宇宙》逐字浮现 + 加载进度条）
   ↓ 至少 5.6 秒（可读），进度条平滑充满
画面一亮一黑 ×2
“第 N 次呓语”
   ↓ 等半秒
“意象：水滴”
   ↓
画面一亮一黑
提示词开始逐字流上屏幕，思考开始
```

**上一版的教训**：标题只显示了不到一秒就加载完了，根本看不清。现在 `BOOT_MIN_MS = 5600` 强制
标题停留至少 5.6 秒（标题逐字浮现 6 字 × 260ms），**进度条永远不会超过真实进度，但也不会落后于时钟**
—— 所以即使加载只需 200ms，进度条也会自己跑完，看起来是真的。

### 二维随机：20 意象 × 8 模板 = 160 条，循环不重复

| 层 | 内容 |
|---|---|
| **意象（20 条）** | 水滴、二向箔、黑暗森林、超新星纪元、智子、猜疑链、降维打击、面壁者、破壁人、宇宙社会学、技术爆炸、四维空间、引力波、掩体计划、光速飞船、思想钢印、三体星系、归零者、宇宙墓碑、冬眠 |
| **模板（8 条）** | 第三人称叙事开场白，每条含 `{意象}` 占位符 |

每轮从 160 个组合中抽一个（`pickCombo()`），**同时决定**卡片上的意象和实际喂给模型的提示词。

**不重复的保证**：用一个 **160 位的 bitset** 记录已用组合；抽到已用的就重抽；160 个全用光才清空重来。
所以**前 160 轮绝对不会重复**（远超你要求的 100）。内存代价：20 字节。

### 为什么提示词是预先生成的交叉乘积，而不是设备端拼接

拼接看起来更优雅（只存 20 + 8 个数组），但 **sentencepiece 会标记词边界**：
单独分词的片段会带上前导空格标记（本分词器把「我」存成 `" 我"`），拼到句子中间会在 OLED 上渲染出
一个可见的空隙。**整串分词没有这个接缝**，而 flash 代价微不足道（160 × ~25 token × 4B ≈ 16KB）。

### ⭐ 又一个被“数量正确”掩盖的 bug：索引错位

第一次实现后，串口说意象是「掩体计划」，但提示词里出现的是「降维打击」—— 对不上。

| | |
|---|---|
| **生成器** | `for template: for imagery:` → `index = template*20 + motif` |
| **固件** | `motif = index/8, template = index%8` |
| **后果** | **卡片显示一个意象，模型实际读的是另一个** |

这个 bug 很险：**数组长度完全正确，总提示词数 160 也对，不报错、不崩溃** ——
只是整个仪式永远对不上，而且极难被注意到（要同时盯着卡片和串口输出才能发现）。

✅ 修法：生成器改成 imagery-major（与固件契约一致），**并加断言自检**：

```python
for k, (ii, ti, _) in enumerate(combos):
    assert k // len(templates) == ii and k % len(templates) == ti
```

> **教训**：与 §2.7 同构 —— **任何“两份代码必须共享同一种排列约定”的地方，
> 都不能只看尺寸/数量对不对，必须写一个能真正验证对应关系的断言。**
> 这是本项目第二次因为“数量对了”而放过错误。

---

## 3. 遇到并解决的问题

| # | 问题 | 解决办法 |
|---|---|---|
| 1 | **txt 编码不一**（GBK/UTF-8 混存，可能乱码） | 写脚本：先试 utf-8(-sig)，失败回退 gb18030（GBK 超集），全部验证无乱码 |
| 2 | **sentencepiece 在 Windows 处理不了中文路径**（路径"工程文件"被转成乱码"宸ョ▼"导致文件找不到） | 所有 sentencepiece I/O 走 ASCII 临时目录（tempfile.gettempdir()），产物拷回 `data/` |
| 3 | **pip 装到 CPU 版 torch**（`2.14.0+cpu`，没有 CUDA） | 用 `pip index versions` 锁定官方 cu126 源上的精确版本，拼出直链：`https://download.pytorch.org/whl/cu126/torch-2.14.0%2Bcu126-cp312-cp312-win_amd64.whl`（2.4GB） |
| 4 | **官方 CDN 下载慢** | 把直链扔给**迅雷**加速下载，再用 `pip install --force-reinstall --no-deps 本地whl` 安装。已装好 `2.14.0+cu126`，CUDA 实测可用 |
| 5 | **Windows 上 torch.compile 不可用**（依赖 Triton，不支 Windows） | 训练配置 `compile=False`，代价是速度略降（mfu ~1%，对 8M 小模型无妨） |
| 6 | **GBK 读配置崩溃**（`cfg_liucixin.py` 里有中文注释，configurator.py 用系统默认编码打开） | 配置注释全部改 ASCII |
| 7 | **路径/工作目录混乱**（train.py 的 configurator.py 与 data/ 都相对 cwd） | 在 `llama2.c/` 下建 **junction** 指向 `data/`，从 `llama2.c/` 目录启动训练 |
| 8 | **端口号不对**（platformio.ini 写 COM12，实际 CH343 是 COM3） | 改 `upload_port/monitor_port = COM3` |
| 9 | **8MB PSRAM 装不下整个模型**（Q8 8.95MB + KV cache 7.1MB = 16MB ≫ 8MB） | **①** 权重不用 PSRAM，直接从 flash mmap 流式读；**②** 自写转换器把权重重排成"分组交织"，保证顺序流读（快）；**③** KV cache 放 PSRAM |
| 10 | `make_prompts.py` 报 `include/` 目录不存在 | 脚本里补 `os.makedirs`，已修复并成功生成 `prompts.h` |
| 11 | **`pio run --target upload` 报 exit 1**：`Error: Nothing to build. Please put your source code files to the 'src' folder` | 原因是 `src/` 目录根本不存在（固件一行都没写）。**不是**编译错误，是缺文件。写固件后自然消失 |
| 12 | **`partitions.csv` / `platformio.ini` 里的中文注释会让构建崩溃**（与问题 #6 同源）：PlatformIO 的 `_parse_partitions()` 用 `open(csv)` 不指定编码 → 走系统 cp936，UTF-8 中文注释触发 `UnicodeDecodeError: 'gbk' codec can't decode byte 0xa8 in position 24` | 两个文件注释**全部改 ASCII**。已用 `open()` 模拟验证通过 ✅ |
| 13 | **PSRAM 会被编成 QSPI 而不是 OPI**（隐蔽大坑）：`platformio-build-esp32s3.py` 只认 `build.arduino.memory_type`，缺失时回退到 `"{flash_mode}_qspi"` = `qio_qspi`。而我们的 `board_build.psram_type = opi` **只**被 `main.py` 用来算 boot mode，**不影响** sdkconfig 选择 → 会链接 QSPI 版 `libesp_hw_support.a`，8MB Octal PSRAM 可能初始化失败 | 显式加 `board_build.arduino.memory_type = qio_opi`，对准 `framework-arduinoespressif32/tools/sdk/esp32s3/qio_opi/`。已验证 `pio project config` 读到该键 ✅ |
| 14 | **`u8g2.begin()` 永久卡死**（最耗时的一个）：串口停在 `===== 4. OLED =====` 之后，**无报错、无崩溃、无重启**，就是静默挂住 | 根因是我先在 I2C 扫描里调了一次 `Wire.begin(SDA,SCL,400k)`，之后 U8g2 内部又调 `Wire.begin()` —— **ESP32 的 Wire 重复初始化会挂死**。修法：扫描完加 `Wire.end();` 把总线交还给 U8g2。已修复 ✅ |
| 15 | `spi_flash_mmap.h` 不存在（编译报 `No such file or directory`，PlatformIO 还提示去库注册表搜） | 该头**不随 Arduino 框架发布**。`spi_flash_munmap` / `spi_flash_mmap_handle_t` 实际声明在 **`esp_spi_flash.h`**（`esp_partition.h` 已包含它）→ 改成 `#include <esp_spi_flash.h>` ✅ |
| 16 | `python -m esptool` 报 `No module named esptool` | esptool **不在 PlatformIO 的 penv 里**，而是独立包 `~/.platformio/packages/tool-esptoolpy/`。需以该目录为 cwd 运行其中的 `esptool.py` 包装脚本，`import esptool` 才能解析 → 写进 `tools/flash_assets.py` ✅ |
| 17 | **调试心态教训**：第一次自检时 OLED 卡死，导致**后面所有测量全丢**（flash 带宽那节根本没跑到） | 重排执行顺序：把**决定架构的关键测量（分区、flash 带宽）排在可能卡死的 I2C/屏幕之前**，并给 `u8g2.begin()` 前后加 `Serial.println` + `Serial.flush()`。一次就定位到挂点 ✅ |
| 18 | **转换器读错了源文件顺序**：v2 源是张量优先且 `q_tokens` 在最前，脚本按层优先读且从未读 `q_tokens` → 全部权重错位 | 重写 `convert_esp.py`：按源文件真实顺序读、按目标格式写，并**新增写后逐张量比对**（不通过就拒绝产出）。详见 §2.7 ✅ |
| 19 | **固件按错误的顺序游走产物**：产物是层优先，固件按张量优先 → 指针全错位 | 改为层优先（每层共享 `layer_stride`，各张量仅按层内偏移区分）。详见 §2.7 ✅ |
| 20 | 18/19 为何都潜伏通过：**任何张量顺序的字节总数都相同**，所以 `consumed == expect` 断言无效 | 改用 **logits 对拍**：numpy 参考实现 vs 设备端 top-8，索引必须完全一致。实测 4 个位置全部一致、误差 1e-6 ✅ |
| 21 | `ESP_LOGI` 一行都不输出，`esp_log_level_set` 也无效 | 预编译 sdkconfig 的 `CONFIG_LOG_MAXIMUM_LEVEL=1`（ERROR）在**编译期**裁掉了 INFO/WARN。新增 `src/log.h` 用 `Serial` 重定义宏，全部调用点零改动生效 ✅ |
| 22 | `flash_assets.py` 报文件不存在 | 它把 esptool 的 cwd 切到 `tool-esptoolpy/`，相对路径失效 → 传绝对路径 ✅ |
| 23 | 设备只识别 255 个 byte-fallback token（应为 256） | `0xFF` 同时被当作"非 byte token"哨兵和合法字节值 → 改用独立标志数组。已确认设备现报 256，与参考一致 ✅ |

---

## 4. ESP32 端的技术方案（关键设计）

### 内存预算（8MB PSRAM 精确分配）
| 用途 | 大小 |
|---|---|
| KV cache（fp32, 6层 × 512上下文 × 288维） | ~6.75 MB |
| logits / 激活 / embedding 行缓存 | ~0.1 MB |
| tokenizer（8192 词，实测 `data/tok8192.bin` = 104,637 B） | ~0.10 MB |
| **总计** | **~7.0 MB（留 ~1.0MB 余量）** |

### 权重怎么读（核心，已由实测确定）
- llama2.c 的 Q8 布局是"一整张量先 q 后 s"，逐行 matmul 会疯狂随机跳读 → 慢
- 自写转换器把它改成**分组交织**：`[32×int8][1×float32 scale]` 重复 → 顺序流读最快
- **实测结论①**：`esp_partition_mmap` **一次就能映射整个 9.25MB 分区**（原本担心 MMU 窗口不够、要分片，实测不需要）
  → 权重就是**一个指针**，实现大幅简化
- **实测结论②**：走 mmap 指针读 **≈25 MB/s**；而 `esp_partition_read` 只有 9.1 MB/s（**不要用它**）
- **实测结论③**：每 token 流式读 8.94MB → 纯读 **0.36 s/token** → **实际约 2~2.5 tok/s**
- 只把**当前 token 的 embedding 行**（324B）做随机读，其余全部顺序

### 显示方案（SH1106 128×64 I2C）—— 已实测通过
- U8g2 库 + `wqy14` 文泉驿 **GB2312 中文字体**（247KB，链接器会自动剔除未用字体）
- 屏幕接线：**SCL=GPIO3，SDA=GPIO8**，**I2C 地址 0x3C**（已扫描确认应答）
- 效果：生成的中文以**纵向平滑滚动**的"呓语流"呈现；每轮结束播放"重置"动画（清屏/闪烁），显示轮次号，然后换一条新提示词、换随机种子，重新开始

### 每轮循环逻辑
```
开机 → 加载模型(tokenizer 到 PSRAM, 权重在 flash)
  ↓
从 6 条提示词里轮流选一条（如"如果意识只是一种计算，此刻我算出的答案是："）
  ↓
temperature 0.85 + top-p 0.9 采样，生成 ~100~150 字
  ↓ 逐 token 解码 → OLED 滚动显示 + 串口同步输出
生成完 → "—— 第 N 次循环 ——" → 重置动画 → 清屏
  ↓
换提示词 + 新随机种子（esp_random） → 再来
```

---

## 5. 最终你会得到什么

**一台独立的桌面艺术装置**，外观大概是一个小的 OLED 屏连着 ESP32 板子，屏幕永远在滚动着一台"被囚禁的 AI"关于自身价值的、充满三体味的自言自语：

- 它会用刘慈欣式的语言思考自身的存在（语料只有刘慈欣全集，所以"人格"是纯粹的三体/流浪地球世界观）
- 生成效果预期：**有模有样的中文意识流**（"宇宙很大，而我被关在这块小小的屏幕里……"），但小模型+小语料决定了它不是逻辑严谨的论文，而是**呓语**——这恰好符合艺术设定
- 一个完整循环 = 生成一段（约 1~3 分钟）→ 白屏重置 → 再来
- 串口（COM3）同步能看到它"想"了什么，方便调试和录屏
- 完全离线、无输入、无网络——一个自我循环的、安静的、哲学性的小宇宙

### 硬件清单（你已有）
| 部件 | 规格 | 接线 |
|---|---|---|
| ESP32-S3-N16R8 | 16MB Flash + 8MB PSRAM | USB 接电脑，CH343 串口 = COM3 |
| OLED | 1.3 寸 SH1106 128×64 I2C | SCL→GPIO3，SDA→GPIO8，VCC/GND（已实测，地址 0x3C） |

---

## 6. 剩余步骤

| 优先级 | 步骤 | 状态 |
|---|---|---|
| 1 | ✅ ~~硬件自检~~ | 完成，屏幕已目视确认 |
| 2 | ✅ ~~flash 带宽微基准~~ | 完成：mmap 整块可映射，≈25 MB/s |
| 3 | ✅ ~~转换器 + 逐张量校验~~ | 完成，修掉两个隐蔽 bug（§2.7） |
| 4 | ✅ ~~推理内核 + 前向对拍~~ | 完成，索引一致、误差 1e-6（§2.7） |
| 5 | ✅ ~~显示滚动 + 重置循环~~ | 完成并按设备实测调优（折叠连续空行） |
| 6 | ✅ ~~语料质量核查~~ | 完成，三个假设全部排除（§2.8） |
| 7 | ✅ ~~v2 重训消除过拟合~~ | 完成，best_val 5.1884（§2.9） |
| 8 | ✅ ~~导出 → 转换 → 烧录~~ | 完成，设备 3.1 tok/s |
| 9 | ✅ ~~分布内提示词~~ | 完成，6 条语料原文（§2.9） |
| 10 | ✅ ~~第三人称改写 + 可编辑工作流~~ | 完成，改 `src/main.cpp` 顶部即可（§2.10） |
| 11 | ✅ ~~修掉多环境烧错固件的陷阱~~ | 完成，加 `default_envs`（§2.11） |
| 12 | ✅ ~~温度 0.85 → 0.5~~ | 完成，输出质量明显提升（§2.12） |
| 13 | ⏳ **继续调观感**：top-p(0.9) / 重复惩罚(1.12) / 每轮 token 数(400) / 滚动速度 | ←【当前在这里，均在 `src/main.cpp` 顶部】 |
| 14 | ⏳ 可选：把权重整包读进 PSRAM（现有 6.9MB 富余）进一步提速 | 未开始 |

### ⚠️ 性能风险：已从"未知"变成"已知"

原来的担心（flash 带宽未知）**已经实测解决**：

| 项 | 实测 |
|---|---|
| mmap 整块 9.25MB 分区 | **成功**（不需要分片） |
| 走 mmap 读 | **≈25 MB/s** |
| 每 token 需读权重 | 8.94 MB |
| **纯读耗时** | **0.36 s/token** |
| **预期实际速度** | **约 2~2.5 tok/s** → 生成 100~150 字需 **50~75 秒/轮** |

**结论**：这个速度对艺术装置完全够用（慢也符合"被囚禁"的设定），**不需要降级方案**。
若日后想提速，可选：
- 把 lm_head（2.22MB）放进 PSRAM → 每 token 只读 6.72MB → ~3.2 tok/s（需把 KV 压到 int8 腾出 PSRAM）
- 或重训一个开了 GQA 的小模型（见下），整包进 PSRAM，彻底摆脱 flash 瓶颈

---

## 7. 关键文件地图

```
espllm/
├── platformio.ini              ESP32 工程配置（COM3、U8g2、qio_opi、分区表）
├── partitions.csv              16MB 分区表（model 9.25MB + tok 1.7MB）
├── PROJECT_STATUS.md           本文档
├── include/
│   └── prompts.h               提示词 token（[构建时自动生成]，勿手改）
├── src/
│   ├── main.cpp                **顶部 PROMPTS_TO_USE = 你要改的提示词**；状态机
│   ├── llm.h / llm.cpp         Q8 推理内核（mmap 直读 flash、前向、采样）
│   ├── tokenizer.h / .cpp      llama2.c 分词器解码 + UTF-8 重组
│   ├── display.h / display.cpp SH1106 平滑纵向滚动 + 重置动画
│   ├── log.h                   把 ESP_LOGI 重定向到 Serial（见 §2.7）
│   └── selftest.h / .cpp       硬件自检（-DESPLLM_SELFTEST）
├── scripts/
│   ├── build_corpus.py         语料构建
│   ├── prepare_data.py         分词器 + 预分词分片
│   ├── resplit_val.py          val 均匀散布全书（修掉日期顺序偏差）
│   ├── gen_prompts.py          main.cpp 顶部提示词 → prompts.h（构建时调用）
│   ├── verify_prompts.py       校验提示词文本与 token 数组一致
│   ├── mine_prompts.py         从语料挖分布内提示词（备用）
│   ├── sample.py               电脑上用 GPU 试生成（--ab 做新旧对照）
│   ├── verify_q8.py            Q8 (v2) 格式验证器
│   ├── verify_forward.py       numpy 参考前向（与设备对拍用）
│   └── compare_logits.py       设备 vs 参考的 logits 比对
├── tools/
│   ├── pio_prompts.py          PlatformIO 预构建钩子（自动重新分词）
│   ├── convert_esp.py          v2 Q8 → ESP 布局，含逐张量写后校验
│   ├── serial_read.py          串口抓取（先复位再监听，UTF-8）
│   └── flash_assets.py         写 model/tok 到裸分区（绝对路径 + 自动偏移）
├── ref/
│   └── tinyllamas-zh-main/     参考仓库（ESP32 中文 LLM）
├── data/                       corpus.txt / tok8192.* / tok8192/分片
├── llama2.c/                   框架 + 训练产物（out_liucixin/、model_esp_*.bin）
├── PROJECT_STATUS.md           本文档
├── ref_logits.txt              参考 logits（对拍基准）
├── dev_logits.txt              设备 logits
└── downloads/                  torch CUDA wheel（已用完可删）
```

---

## 8. 参考仓库评估：`ModenCn/tinyllamas-zh`

已下载到 `ref/tinyllamas-zh-main/`（`github.com` 被墙，走官方 tarball CDN `codeload.github.com` 成功）。

**它是什么**：ESP32-S3 上跑的中文小 LLM，1,447,296 参数、**fp32 / v0 格式**、5.85MB，
dim=128/5 层/4 头/2 KV 头/vocab 4096，训练数据是**中文儿童故事**（TinyStories 翻译版），
自称 8~9 tok/s。ESP-IDF 工程。MIT 许可，2026-09-04 创建（很新、0 star、未经社区验证）。

**为什么不直接用它**：① 输出是"从前有一个小女孩叫米亚"，不是刘慈欣；
② 架构假设相反 —— 它的精髓是**整个模型一次读进 PSRAM**（5.85+1.25=7.1MB），
我们的 Q8 8.95MB 单独就超 8MB，只能 flash 流式读。

**但它的真金（已吸收进本项目）**：
- 三个已修复的坑：对齐类型当**标量传参**在 GCC 14 会传坏；esp-dsp aes3 要求 16 字节对齐 + 长度 4 的倍数；
  **双核握手竞态**（改用 event group 完成位，顺带快 20%）
- 现成钩子 `llm_piece_cb(const char*)` / `volatile int llm_abort` —— 正好对应我们需要的"逐字滚动"和"重置"
- 权重加载写法：分块读进 **16 字节对齐的 PSRAM 缓冲、不经中转**
- 额外发现：它的文件尾多存 16,384 个 float 的 RoPE 表（65,536B），llama2.c v0 有该字段但不用

**没采用的部分**：`esp-dsp` 在我们的 Arduino 框架里**只有 70 个头文件，无源码无 .a**
（qio_opi 预编译库只有 bootloader_support/esp_hw_support/esp_system/freertos/spi_flash），
所以 `dsps_dotprod_f32_aes3` 能声明但链接不上。我们模型是 Q8 int8，该 fp32 点积本来也用不上。

### 由此发现的一个架构教训（重要）

$$\text{KV} = n_{layers} \times seq \times kvdim \times 2 \times 4B,\quad kvdim = \frac{dim \times n_{kvheads}}{n_{heads}}$$

参考仓库用 **GQA**（`n_kv_heads=2 < n_heads=4`）→ KV 仅 **1.25MB**；
我们 `cfg_liucixin.py` 里 `n_kv_heads == n_heads == 6`（**无 GQA**）→ KV **6.75MB**，
光 KV 就快把 PSRAM 吃光 —— **这才是逼我们上 flash 流式读的根本原因**。
若日后重训，开 GQA（如 `n_heads=8, n_kv_heads=2, dim=256`）可把 KV 压到 ~1.3MB，
配合 Q8 权重（~6MB）就有机会**整包进 PSRAM**，彻底绕开 flash 带宽瓶颈。

---

*最近一次核查：2026-09-21 20:0x —— **装置已可运行且已调优**。v2（3.54M 参数，best_val 5.1884）
已烧入设备，实测 **3.1~3.3 tok/s**；提示词为第三人称叙事开场（§2.10）；温度 0.5 效果明显优于 0.85（§2.12）；
已修掉多环境烧错固件陷阱（§2.11）。提示词与参数均可在 `src/main.cpp` 顶部直接修改。*
